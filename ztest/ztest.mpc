sfix.set_precision(9, 10)

mean1 = sfix(5.5)
mean2 = sfix(5.5)

denominator = sfix(5.5)
# the denominator, which bases on only public information

T = (mean1 + mean2) / denominator
cv = sfix(12.2)

res = cv > T